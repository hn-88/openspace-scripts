This needs global illumination, else one of the poles would be in darkness (except for the Martian equinox, I guess.)

`openspace.action.triggerAction("os.AllGlobesGlobalIllumination")`

In profiles other than the default profile, this would need this asset to be loaded,

`data/assets/actions/planets/planet_lighting.asset`
