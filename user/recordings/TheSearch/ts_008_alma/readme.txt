The Alma zoom in files created by concatenating keyframes had two issues:
1. I had not created sufficient number of keyframes, so the camera moved through Earth.
2. When zooming in, waiting for the textures to load was causing crashes.

So, used the manual zoom out at 15 fps instead, to create a 30 second 30 fps zoom-out.
