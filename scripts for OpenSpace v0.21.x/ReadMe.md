## Older scripts

OpenSpace 0.22.0 has some api changes, so the "button-pages" html-js scripts written for OpenSpace 0.21.x would need changes. The `openspace-api.js` file has also changed, its repo is https://github.com/OpenSpace/openspace-api-js
